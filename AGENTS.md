# AGENTS.md

## Project Overview
Aplikasi web Kalkulator Status Gizi Anak (Posyandu) berbasis standar pertumbuhan WHO
(metode LMS). Bahasa: Indonesia (kode, komentar, dan UI).

Arsitektur saat ini: **SPA Vite + Vue 3** dengan perhitungan **client-side (TypeScript)**,
backend masa depan memakai **Supabase** (BaaS). Rencana pengembangan jangka panjang
terdokumentasi di **PRD.md** (living document); jaga agar AGENTS.md dan PRD.md tetap sinkron.
Panduan ringkas untuk Claude Code tersedia di **CLAUDE.md** (perintah, arsitektur big-picture,
konvensi keras) — jagalah tetap sinkron dengan file ini.

## Tech Stack
- Frontend: Vue 3 SPA + TypeScript + Vite 8 (bundler), Vue Router (SPA, lazy routes)
- UI: shadcn-vue (komponen `src/components/ui/`) + Tailwind CSS v4 (`@tailwindcss/vite`)
- Ikon: `@lucide/vue`; Ekspor rekap: SheetJS `xlsx` (client-side, **impor dinamis** — chunk terpisah, hanya dimuat saat ekspor; versi 0.18.5 dipertahankan — lihat catatan accepted-risk #14 di "Masalah Dikenal")
- Perhitungan: TS port dari kalkulator Python (metode LMS WHO, z-score) di `src/lib/kalkulator/`
- Font: **IBM Plex Sans** (formal, variabel 400–700, bundel woff2 lokal di `public/fonts/`) — dipakai untuk display & body (konsisten, mudah dibaca semua orang)
- Backend: Supabase (PostgreSQL + Auth/RLS) — fase data, lihat PRD; `@supabase/supabase-js` sudah terpasang
- PWA: `vite-plugin-pwa` (autoUpdate) sejak v2.27.0 — app shell ter-precache (bisa dibuka offline); **API Supabase tidak pernah di-cache**; ikon PNG di `public/pwa-*.png` + `maskable-icon-512x512.png`
- Deploy: **GitHub Pages** aktif (lihat workflow + rincian di bawah); **Vercel** target final (SPA rewrite di `vercel.json` sudah siap)
- Test: Vitest (unit kalkulator/status/bumil/kbm/galat/offlineAntre/lapisan `db.ts` + rekap + smoke test render komponen)
- Lint: ESLint 9 flat config (`eslint.config.ts`) + typescript-eslint 8 + eslint-plugin-vue 10 — `npm run lint`

## Setup & Perintah
- `npm install`                        # pasang semua dependensi
- `npm run dev`                        # dev server (port 5173)
- `npm run build`                      # vue-tsc typecheck + vite build → `dist/`
- `npm run preview`                    # preview hasil build
- `npm test`                           # vitest run (kalkulator + status/bumil + rekap + db lapisan data + offline + render; baseline 161 hijau + 1 skips bersyarat)
- `npm run lint`                       # eslint . (wajib bersih sebelum commit)
- Uji kalkulator cepat: `npx vitest run src/lib/kalkulator`
- `npx supabase link --project-ref <ref>` # tautkan repo ke proyek Supabase
- `npx supabase db push`               # terapkan migrasi `supabase/migrations/` ke remote (meminta password DB)
- `.env.example`                       # templat env (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_APP_URL)
- Deploy GitHub Pages: `npm run build -- --base=/posyandu/` lalu push `main` → workflow `.github/workflows/deploy-pages.yml` (secret `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`/`VITE_APP_URL` diteruskan ke build) otomatis deploy ke `gunawan-ly.github.io/posyandu` (deep-link ditangani `public/404.html` + restore di `src/main.ts`)
- Alias import: `@` → `src` (vite.config.ts + tsconfig paths)
- Sumber data `tabel.ts`: skrip konversi CSV **tidak dicommit** — regenerasi manual (buat skrip ad-hoc dari `refrences/*.csv`, lalu jalankan `npm test`)

## Struktur Proyek
- `index.html` — entry HTML SPA (mount `#app`); `public/` (favicon, font woff2)
- `src/main.ts` — bootstrap app + router
- `src/router/index.ts` — rute `/` (landing), `/kalkulator`, `/login`, `/dashboard` (publik) + rute modul via spread (mis. `...balitaRoutes` dari `src/modules/balita/routes.ts`); guard `requiresAuth` + `requiresAdmin` di `beforeEach`; lazy-load, fallback `*` → `/`; `scrollBehavior` (scroll ke anchor `/#indikator`, `/#tentang`; selain itu ke atas; hormati `prefers-reduced-motion`)
- `src/modules/` — **struktur per-modul** (fondasi multi-posyandu): tiap modul punya `views/` (list/form/detail), `db.ts` (lapisan data modul), `routes.ts` (rute + meta guard). Saat ini **modul `balita`, `bumil`, `apras`, dan `remaja` aktif** (remaja: identitas + CRUD sejak v2.39.0, kunjungan sejak v2.40.0, rekap menyusul); **`lansia`** masih placeholder dengan rute + shape `db.ts` terdaftar di router (rincian di bagian bawah daftar modul). Modul baru cukup menyalin *shape* folder ini — tambah `...<modul>Routes` di router tanpa mengubah struktur inti. Jangan menumpuk service/views modul baru di `src/views/` atau file `db.ts` app-level — taruh di `src/modules/<modul>/`.
  - `src/modules/balita/views/` — `BalitaListView.vue` (daftar pakai composable `useDaftarModul`; tambah/ubah identitas hanya via `FormModalBalita`; sejak v2.30.0 tautan `/balita?tambah=1` otomatis membuka modal tambah — dipakai CTA Dashboard), `BalitaDetailView.vue` (identitas + kurva tabs + riwayat + form kunjungan lengkap; tombol **Ubah** membuka `FormModalBalita` — konsolidasi v2.30.0, halaman form lama dihapus; riwayat **satu tabel dengan scroll horizontal internal** (min-w tinggi) diurutkan **kronologis naik** (Januari→Desember), status LiKA/LiLA dihitung otomatis saat form kunjungan diisi (preview memakai `hitungUmurBulan` — umur kalender, sama dengan logika penyimpanan); pencarian daftar mencocokkan `nama`/`nama_orang_tua`/`nik`; grid item kiri wajib `min-w-0` agar halaman tidak melebar horizontal; sejak v2.11.2 kerangka yang memakai komponen anak di `views/detail/`: `FormKunjunganBalita.vue` (props `balita`+`isAdmin`+`edit?`+`riwayat?`, emit `tersimpan`; mode edit me-render dalam Dialog dari induk; sejak v2.26.0 penilaian BB naik otomatis via KBM — kotak preview kenaikan vs KBM + auto-isi pilihan yang tetap bisa dioverride manual, flag manual reset saat tanggal/BB berubah; sejak v2.34.0 semua `<select>` diganti `InputSegmen` — 8 pilihan 2-opsi termasuk BB naik, ceklis perkembangan (dengan `display` map L→Lengkap/TL→Tidak Lengkap), imunisasi, vitamin A, ASI, MP-ASI, obat cacing, gejala TBC; urutan LiKA sekarang sebelum LiLA), `TabelRiwayatBalita.vue` (props `kunjungan`+`isAdmin`, emit `hapus`+`ubah`+`lihat`; tombol Ubah/Hapus per baris untuk admin), & `KurvaTabsBalita.vue` (props `balita`+`kunjungan`)), `BalitaRekapView.vue` (rekap bulanan: filter mode **Bulanan** (bulan+tahun) / **Rentang** (tanggal bebas), kartu ringkasan, tabel **format resmi Rekap Bulanan Posyandu** (32 baris kategori via `BARIS_RINGKASAN`; tanpa scroll horizontal di mobile — kolom Keterangan wrap alami), tabel rincian per balita (scroll horizontal), aksi **Ekspor Excel** (`xlsx`), **Salin CSV**, **Cetak/PDF** (`window.print` + CSS `@media print`; class `print-only`/`print-area`/`no-print`))
  - `src/modules/balita/rekap.ts` — logika murni rekap (tanpa Supabase): `filterKunjunganPeriode`/`filterKunjunganRentang`, `rekapPerBalita` (satu suara per balita, kunjungan terakhir), `klasifikasiSasaran` (bayi < 12 bln / balita 12–60 bln), `hitungRekapBulanan` (sasaran, kehadiran bayi & balita terpisah, ceklis, BB naik, status Normal/Tidak Normal per indikator, layanan Ya/Tidak; **aturan rekap baku:** nilai terisi & bukan Ya/Normal → "Tidak"/**"Tidak Normal"**, nilai kosong/tak diisi → **tidak masuk hitungan kedua kolom**, berlaku untuk semua pasangan Ya/Tidak, Normal/Tidak Normal, dan Naik/Tidak Naik), `susunBarisRekap`; interface `RekapBulanan`/`BarisRekap`/`PeriodeRekap`. **Sejak v2.32.0 rekap GABUNGAN Balita+Apras**: tipe `AnakGabungan`/`KunjunganGabungan` ber-kunci `"modul:id"` (anti tabrakan id antar tabel), `gabungAnakBalita`/`gabungKunjunganBalita`, `satuSuaraPerAnak`; anak Apras (>60 bln) dilebur ke keranjang **Balita**; `susunBarisApras` utk baris rincian tanpa z-score; mapper Apras ada di `apras/rekap.ts`; kolom Modul ditambahkan di tabel rincian & lembar ekspor (`BARIS_RINGKASAN` tidak berubah — angka otomatis termasuk Apras). **Penting (v2.38.0):** `hitungRekapBulanan` memfilter kunjungan per periode (bulan atau rentang) SEBELUM `satuSuaraPerAnak` — tanpa ini, satu suara per anak berlaku silang-bulan (anak yang kunjungan terakhirnya di bulan X dihitung "hadir" di SEMUA bulan). Jangan hapus filtering ini.
  - `src/modules/balita/ekspor.ts` — `BARIS_RINGKASAN` (sumber tunggal pasangan label+ambil untuk sheet & tabel UI), `susunLembarRingkasan`/`susunLembarRincian`, `buatWorkbookRekap` (sheet Ringkasan + Rincian; **async** — SheetJS dimuat via impor dinamis, sejak v2.14.4), `unduhXlsx`, `teksCsvRekap` (**async**), `labelPeriode`
  - `src/modules/balita/db.ts` — service CRUD balita & kunjungan (`tambahKunjungan`/`ubahKunjungan` memakai builder bersama `susunIsiKunjungan` — status dihitung ulang dengan alur sama); status dihitung kalkulator TS lalu disimpan sebagai label Indonesia; plus fungsi dashboard `kunjunganTerakhir()` (RPC `kunjungan_terakhir`) & `balitaPerluPerhatian()` (filter status buruk/kurang) untuk seksi "Balita yang perlu perhatian"
  - `src/modules/balita/routes.ts` — rute `/balita*` (`requiresAuth` + `requiresAdmin` di meta); `/balita/rekap` hanya `requiresAuth` (rekap read-only, semua kader)
  - `src/modules/bumil/views/` — `BumilListView.vue` (daftar pakai composable `useDaftarModul`; pencarian mencocokkan `nama`/`nama_suami`/`nik`/`dusun`; tambah/ubah via `FormModalBumil`; tombol **Rekap Tahunan** di header), `BumilDetailView.vue` (identitas + tombol **Ubah** membuka `FormModalBumil` sejak konsolidasi v2.30.0 + riwayat kunjungan tabel scroll horizontal — usia kehamilan bersatuan **minggu** + ringkasan kunjungan terakhir + form kunjungan antenatal lengkap: usia kehamilan, BB & sesuai kurva KIA, LiLA + status hijau/merah, tekanan darah & sesuai kurva KIA, skrining gejala/TBC/TTD/MT KEK/kelas bumil/**vitamin A nifas & KB pasca persalinan (kolom baru v2.31.0, tampil saat kategori Menyusui)**/edukasi/rujuk; field khusus kehamilan disembunyikan saat kategori **Menyusui**; kerangka yang memakai komponen anak di `views/detail/`: `FormKunjunganBumil.vue` (form antenatal; props `bumil`+`isAdmin`+`edit?`, emit `tersimpan`; sejak v2.34.0 semua 16 `<select>` diganti `InputSegmen` — Ya/Tidak, BB/TD kurva KIA, status LiLA; sejak v2.38.2 punya field **Kategori** `InputSegmen` (wajib) untuk kunjungan — `sedangHamil` kini dilihat dari kategori kunjungan ini, bukan identitas) & `TabelRiwayatBumil.vue` (props `kunjungan`+`isAdmin`, emit `hapus`+`ubah`+`lihat`; kolom **Kategori** sejak v2.38.2))
  - `src/modules/bumil/rekap.ts` (v2.31.0) — logika murni **Rekap Tahunan Bumil/Busui format resmi puskesmas**: baris = 12 bulan, kolom = sasaran/datang/tidak datang per kategori (Hamil vs Nifas/Menyusui), BB/LiLA/TD hijau-merah, bergejala TBC (≥2 dari 3 gejala skrining; kontak TBC tidak dihitung), TTD & PMT KEK (dapat + konsumsi 'Ya'→"Setiap Hari"), kelas bumil, vitamin A nifas, KB pasca persalinan, edukasi, rujuk per kategori. **Sejak v2.38.2 sumber kategori ada ganda**: (a) `bumil_kunjungan.kategori` — **sumber kebenaran REKAP** (historis per kunjungan); (b) `bumil_identitas.kategori` — status **saat ini**, diisi manual kader untuk tampilan & statistik publik, **TIDAK dipakai rekap**. Sasaran per bulan = ibu yang **punya kunjungan** pada/atau sebelum akhir bulan (belum pernah kunjungan = belum sasaran, mulai sejak kunjungan pertama); kategori sasaran/datang/rujuk per bulan = dari **kunjungan terakhir** ibu tsb hingga akhir bulan tsb (`hitungRekapTahunan(kunjungan, tahun)` — **tidak lagi menerima `identitas`**). Migrasi `supabase/migrations/20260828000001_bumil_kategori_di_kunjungan.sql` (diterapkan ke remote sejak v2.38.7) menambah kolom `kategori` di `bumil_kunjungan` + backfill dari identitas; identitas `kategori` tetap ada
  - `src/modules/bumil/ekspor.ts` (v2.31.0) — `GRUP_KOLOM` (satu sumber struktur tabel UI/Excel/CSV), `buatWorkbookRekap`/`unduhXlsx`/`teksCsvRekap` (SheetJS impor dinamis; header grup merged), pola sama dengan ekspor Balita
  - `src/modules/bumil/views/BumilRekapView.vue` (v2.31.0) — halaman `/bumil/rekap` (`requiresAuth`, rute sebelum `/:id`): pilih tahun, tabel resmi 12 bulan × 28 kolom bergrup + baris JUMLAH, aksi Ekspor Excel/Salin CSV/Cetak-PDF; migrasi kolom baru di `supabase/migrations/20260826000000_bumil_rekap_kolom.sql` (diterapkan ke remote sejak v2.31.1; penting utk replikasi fresh — insert menyertakan kolom baru)
  - `src/modules/bumil/db.ts` — service CRUD bumil & kunjungan (`tambahKunjunganBumil`/`ubahKunjunganBumil` via builder bersama); **tidak ada z-score WHO utk kehamilan** → status (BB sesuai kurva KIA, LiLA, TD) disimpan sebagai pilihan manual dari konstanta `OPSI_*`; `labelYaTidak` re-export dari `lib/label`; kunjungan terhubung lewat FK `bumil_id` (tabel lama pakai `nama`, migrasi backfill)
  - `src/modules/bumil/routes.ts` — rute `/bumil*` (`requiresAuth` + `requiresAdmin` di meta)
  - `src/modules/apras/` — **modul Apras penuh (Anak Pra Sekolah, usia 5–6 tahun / >60 s.d. <72 bulan; CRUD sejak v2.16.2)**: `AprasListView.vue` (daftar pakai composable `useDaftarModul`; cari + tambah/ubah via `FormModalApras`), `AprasDetailView.vue` (identitas + riwayat kunjungan + form kunjungan tanpa status z-score — referensi WHO hanya sampai 60 bulan), `FormModalApras.vue` (**urutan field sejak v2.30.0**: Nama Anak → NIK → Jenis Kelamin → Tanggal Lahir | Tempat Lahir → Nama Orang Tua | NIK Orang Tua → Nomor KK → Dusun (toggle `InputDusun`) → Alamat → Posyandu; tanpa "Anak ke"; sejak v2.34.0 3 `<select>` diganti `InputSegmen` (Ya/Tidak: obat cacing, gejala TBC, dirujuk)); rute `/apras` (`requiresAuth`); `/apras/baru` & `/apras/:id/edit` **redirect ke daftar** sejak konsolidasi v2.30.0 (rekap gabungan Bayi/Balita/Apras desain menyusul)
  - `src/modules/remaja/` — **modul Remaja aktif (anak usia sekolah & remaja 7–18 tahun; fase 1 = identitas + CRUD sejak v2.39.0; kunjungan sejak v2.40.0, tanpa rekap/z-score otomatis)**: `RemajaListView.vue` (daftar pakai `useDaftarModul`; cari nama/ortu/NIK; tambah/ubah via `FormModalRemaja`; tautan `/remaja?tambah=1` membuka modal tambah), `FormModalRemaja.vue` (urutan field: Nama → NIK → Jenis Kelamin (`InputSegmen`) → Tanggal Lahir | Tempat Lahir → Nama Orang Tua | NIK Orang Tua → Nomor KK → **Nama Sekolah** (satu-satunya field identitas ekstra) → Dusun (`InputDusun`) → Alamat) (kategori Posyandu & kolom `anak_ke` **tidak dipakai** untuk Remaja — kolom `posyandu` di-drop sejak v2.39.2, `anak_ke` sejak v2.39.3), `RemajaDetailView.vue` (sejak v2.40.0: layout 2-kolom ala Apras — kiri **riwayat kunjungan** `TabelRiwayatRemaja`, kanan **Identitas** + **form kunjungan** `FormKunjunganRemaja`; tombol Ubah/Hapus + modal detail kunjungan + dialog ubah kunjungan; admin-only), `views/detail/FormKunjunganRemaja.vue` (props `remaja`/`isAdmin`/`edit?`, emit `tersimpan`; mode edit di-render dari induk; offline via `tambahKeAntre` modul `remaja`; grup: Hasil Penimbangan/Pengukuran — BB, TB, **IMT TUNGGAL baik value+status sebagai TEKS manual, mis. "19 (N)"** (v2.40.1 — tanpa kolom status gizi terpisah), Pemeriksaan (remaja ≥15 tahun, opsional) — lingkar perut, TD sistole/diastole + kategori, gula darah + kategori, Hb + anemia (remaja putri), Skrining gejala TBC (batuk, demam >2 minggu, BB tidak naik 2 bulan + kontak erat; **kontak erat tidak dihitung gejala**, rujuk bila ≥2 dari 3 gejala), Edukasi (free text), Rujuk Ya/Tidak, Catatan; BB & TB wajib), `views/detail/TabelRiwayatRemaja.vue` (props `kunjungan`/`isAdmin`, emit `hapus`+`ubah`+`lihat`; **header bertingkat bergrup ala tabel rekapitulasi (colspan/rowspan)** — kolom tunggal **Tanggal** & **Umur** di awal; **kolom Tanggal di-freeze (sticky left)** agar tetap terlihat saat scroll horizontal & **semua value di-tata tengah (text-center)** sejak v2.40.2; lalu grup induk: Hasil Penimbangan/Pengukuran (BB, TB, IMT), Pemeriksaan (Lingkar perut, **TD value, TD status, Gula value, Gula status** — value & status TERPISAH 2 kolom, Hb, Anemia), Skrining TBC (Batuk, Demam, BB tidak naik, Kontak erat), Layanan (Rujuk, Edukasi/Catatan); kolom aksi admin rowspan 2); `db.ts` berisi `Remaja`/`InputRemaja` + CRUD (`listRemaja` dengan `escapeIlike`, `ambilRemaja`, `buatRemaja`, `ubahRemaja`, `hapusRemaja`) **tanpa status/z-score otomatis** (referensi WHO LMS hanya sampai 60 bulan) + sejak v2.40.0 lapisan kunjungan: `KunjunganRemaja`/`InputKunjunganRemaja` (**26 kolom efektif: `imt` berisi TEKS "19 (N)", tanpa `status_gizi` sejak v2.40.1**; lingkar perut, TD + kategori, gula + kategori, Hb + anemia, 3 gejala TBC + kontak erat, edukasi, rujuk, catatan), `listKunjunganRemaja`/`listKunjunganRemajaPeriode` (kronologis naik), `susunIsiKunjungan` (umur_tahun = `hitungUmurBulan`/12, null-safe, tolak kunjungan sebelum tanggal lahir), `tambahKunjunganRemaja`/`ubahKunjunganRemaja`/`hapusKunjunganRemaja`; **IMT diisi MANUAL oleh kader sebagai teks** (klasifikasi IMT-for-age WHO 2007 menyusul). Migrasi `supabase/migrations/20260829000000_modul_remaja.sql`: tabel `remaja_identitas` + RLS kader baca / admin tulis + trigger `set_dibuat_oleh()`; `20260829000001_hapus_posyandu_remaja.sql` drop kolom `posyandu`; `20260829000002_hapus_anak_ke_remaja.sql` drop kolom `anak_ke`; `supabase/migrations/20260830000001_modul_remaja_kunjungan.sql` (v2.40.0, diterapkan remote): tabel `remaja_kunjungan` (27 kolom) + RLS admin-only + trigger `trg_remaja_kunjungan_dibuat_oleh`; `supabase/migrations/20260831000000_remaja_kunjungan_imt_teks.sql` (v2.40.1): drop kolom `status_gizi` + ubah `imt` numeric→teks. Rute `/remaja*` (`requiresAuth`; `/remaja/:id` detail `requiresAuth`; `/remaja/baru` & `/remaja/:id/edit` redirect ke daftar). **Kunjungan remaja TIDAK punya rekap/bulk bulanan & tidak masuk rekap gabungan Balita+Apras** (menyusul)
  - `src/modules/lansia/` — **placeholder modul Dewasa/Lansia** (struktur menyalin shape modul aktif): `views/*ListView.vue` memakai `PlaceholderModul.vue`; `db.ts` berisi interface + rute untuk pencatatan mendatang. Rute `/lansia*` **terdaftar di router via spread**: list + `tambah=1` → rute bernama `*-baru` (`requiresAuth` + `requiresAdmin` di meta), `/lansia/:id` & `/lansia/:id/edit` (sama utk `/lansia/:id*`) **redirect ke daftar**; guard `requiresAdmin` di `beforeEach` menengahi rute admin semua modul. **TANPA bentuk pencatatan kunjungan** sampai struktur tabel di desain
- `src/views/` — halaman app-level (bukan per-modul): `LandingView.vue` (hero sistem informasi posyandu digital + motto "Sehat & mandiri untuk semua", seksi statistik bulan berjalan via RPC publik `statistik_publik()` (**dipecah per sasaran**: `balita_bayi`/`balita_balita`, `bumil_hamil`/`bumil_menyusui`, persentase kunjungan Balita & Bumil/Menyusui dihitung client-side, `kunjungan_bulan_ini` sebagai badge; fallback `–`/tersembunyi bila DB tak tersedia; latar gradasi blob + kartu kaca glassmorphism ala iOS), seksi layanan 5 sasaran (Bumil, Balita, Apras, & Remaja aktif; Dewasa & Lansia "Segera"), kalkulator kilat interaktif (dipindah dari hero), cara pakai, tentang standar WHO + privasi; **CTA hero sejak v2.14.0:** tombol utama **"Masuk"** cerdas sesi (sudah login → `/dashboard`, belum → `/login`; via `useAuth` + computed `tujuanMasuk`) di atas tengah + di bawahnya navigasi 5 modul posyandu via konstanta `MODUL_NAV` (Bumil & Busui → `/bumil`, Bayi & Balita → `/balita`, Apras → `/apras` sejak v2.15.0, Remaja → `/remaja` sejak v2.39.0; Dewasa & Lansia **terkunci** dengan ikon gembok — klik menampilkan pesan inline `role="status"` "masih dalam tahap pengembangan", auto-hilang 3 detik); sejak v2.11.1 kerangka tipis (49 baris) yang menyusun komponen seksi di `src/views/landing/`: LandingHero/LandingStatistik/LandingLayanan/LandingKalkulatorKilat/LandingCaraPakai/LandingTentang/LandingCta + `kampanye.ts` (flag & teks HUT ter-dedup)), `DashboardView.vue` (perakit tipis ~60 baris yang menyusun komponen seksi: `src/views/dashboard/DashboardHero.vue` (sambutan anon vs kader), `DashboardStatistik.vue` (kartu statistik bulan berjalan), `DashboardLayanan.vue` (navigasi 5 modul), `DashboardPerhatian.vue` (balita perlu perhatian — kader only), `DashboardPrivasi.vue`; statistik pakai composable `useStatistikPublik()` yang juga dipakai `LandingStatistik`; kartu kaca glassmorphism; skeleton saat fetch), `KalkulatorView.vue` (kalkulasi live tanpa tombol hitung: hasil & kurva otomatis saat data lengkap; validasi per-field on-blur dengan `aria-describedby`/`aria-invalid`/live region `role="status"`; tabs kurva BB/U · TB/U · BB/TB; peringatan implausibel |z| > 5 non-blocking; ringkasan + tombol salin), `LoginView.vue` (**hanya masuk** — pendaftaran mandiri dihapus sejak v2.14.0 karena data hanya untuk kader terdaftar; akun dibuat pengelola langsung di database; redirect default `/dashboard`)
- `src/components/` — `KurvaWHO.vue` (kurva WHO self-draw + titik z-score; mode `bbu`/`tbu`/`bbtb`/`lika`/`lila`), `InputDusun.vue` (toggle segmen Dusun — opsi Kayumas/Tengah/Cempaka; nilai lama di luar opsi tetap tersimpan dengan catatan kecil, sejak v2.30.0 dipakai ketiga modal identitas), `InputPosyandu.vue` (toggle segmen Posyandu — Coklat 1/Coklat 2/Luar Daerah), `InputSegmen.vue` (toggle segmen generik; prop `opsi` (nilai mentah), optional `display` (peta nilai→label tampil, v2.34.0); dipakai semua form kunjungan untuk pilihan 2 opsi: Ya/Tidak, Naik/Tidak Naik, Lengkap/Tidak Lengkap, dll — menggantikan semua `<select>` di form kunjungan; juga dipakai untuk Jenis Kelamin di FormModalBalita), `PlaceholderModul.vue` (placeholder generik modul belum aktif — sejak v2.39.0 dipakai Lansia saja, Remaja sudah aktif), `StatusBadge.vue` (label lengkap saja, tanpa kode), `Skeleton.vue` (blok loading animate-pulse untuk halaman fetch data), `AppNavbar.vue` (brand statis "Posyandu Wapalo" **bold**, tautan Beranda/Tentang/Dashboard + dropdown modul **Data Balita** (label dinamis: "Balita" di `/balita*`, "Bumil" di `/bumil*`, default "Data Balita"; tersedia desktop & mobile dalam sheet; menu desktop absolutely positioned agar layout stabil), tab aktif benar utk rute & anchor `#tentang` via IntersectionObserver, frosted glass), `AppFooter.vue` (info © + credit "Dikembangkan oleh Awan" di baris paling bawah; `mt-auto` → sticky di dasar viewport via layout `flex min-h-screen flex-col` di `App.vue`), `Reveal.vue`, `ui/` (komponen shadcn-vue; `Card` punya varian `solid`/`glass`/`glass-strong`)
- `src/lib/kalkulator/index.ts` — port TS `hitungSemuaStatus(jk, umurBulan, beratBadan, panjangBadan)` → `{status_bb_u, status_tb_u, status_bb_tb, z_bb_u, z_tb_u, z_bb_tb, error}`; plus `hitungZLik`/`hitungZLil` (lingkar kepala/lengan) & `klasifikasiLika`/`klasifikasiLila`
- `src/lib/kalkulator/tabel.ts` — data WHO hasil konversi CSV (jangan edit manual): `wfa`, `lhfa*2y/5y`, `wfl/wfh`, `hcfa*`, `acfa*`
- `src/lib/kalkulator/__fixtures__/expected.json` — fixture output Python lama untuk validasi port
- `src/lib/umur.ts` — hitung umur bulan (kalender) + parse tanggal lokal + `umurSaatIni`
- `src/lib/status.ts` — metadata status (label, deskripsi, tone warna) + `labelStatus`/`kodeDariLabel` (kode↔label Indonesia, termasuk varian data lama)
- `src/lib/galat.ts` — normalisasi galat → pesan Indonesia (`pesanGalat`/`lemparGalat`): kode Postgres (42501 RLS, 23505 duplikat, …), pesan auth Supabase, galat jaringan; `adalahGalatJaringan()` dipakai antrean offline. Semua `throw error` Supabase di `db.ts` & `useAuth.ts` melewati `lemparGalat` sejak v2.24.0
- `src/lib/kbm.ts` — tabel Kenaikan Berat Badan Minimal per usia (v2.26.0): nilai tetap 1–6 bln (800/900/800/600/500/400 g), prorata 300 g/bln utk 7–11 bln, 200 g/bln utk ≥12 bln (fallback >60 bln); <1 bln pakai kategori 1 bln. `statusNaikDariTanggal()` membandingkan BB kini vs kunjungan lalu (usia & jarak kalender)
- `src/lib/offlineAntre.ts` — antrean offline pencatatan kunjungan (v2.27.0): FIFO di localStorage (kunci `posyandu-antre-kunjungan`, input mentah tanpa hasil hitung), sync otomatis saat event online/app dibuka/manual; galat jaringan menghentikan sync, galat lain menandai item `gagal`; wrapper storage tahan Storage rusak + cadangan memori sesi. Edit/hapus kunjungan sengaja TIDAK masuk antrean (wajib online)
- `src/lib/label.ts` — helper tampilan bersama: `labelYaTidak` (Y/T → Ya/Tidak; sumber tunggal, re-export dari `balita/db` & `bumil/db`), `labelBulan` (YYYY-MM → "Januari 2026"), `formatUmur` (tanggal lahir → "2 th 3 bln"), `formatTanggal` (→ "1 Jan 2026"), `labelJk` (Jenis Kelamin)
- `src/composables/useStatistikPublik.ts` — composable statistik bulan berjalan (v2.33.0, refactor v2.34.0): fetch RPC `statistik_publik()` + kartu statistik terkelompok: `SASARAN` (5 item: Bayi/Balita/Apras/Bumil/Busui) + `KUNJUNGAN` (3 item: persentase kunjungan Balita/Apras/Bumil) + `KARTU_STATISTIK` (deprecated, gabungan flat). Tipe `KartuStatistik` diekspor. Dipakai `DashboardStatistik` (2-kolom: sasaran+kunjungan) & `LandingStatistik`
- `src/composables/useDaftarModul.ts` — composable daftar modul (v2.33.0): search debounce 300ms, muat data, hapus+retry, view toggle persisten (kartu/tabel), modal tambah/ubah state. Dipakai ketiga `*ListView`
- `src/components/PlaceholderModul.vue` — placeholder generik untuk modul belum aktif (dipakai `LansiaListView`; pindah dari `apras/views/` ke `components/` v2.33.0)
- `src/components/AntreBanner.vue` — pill global (App.vue): status offline / N kunjungan menunggu / gagal kirim, tombol kirim ulang & buang gagal
- `src/supabase/client.ts` — klien Supabase (aktif hanya bila env var terisi) + `wajibSupabase()` (lapisan data per modul)
- `src/supabase/useAuth.ts` — composable auth (session, masuk, daftar, keluar) + `isAdmin` via `rpc('is_admin')`; `signUp` memakai `emailRedirectTo = VITE_APP_URL || origin + BASE_URL`
- `supabase/` — CLI project (`config.toml`) + `migrations/*.sql` (schema, relasi, RLS, fungsi publik); `.temp/` & `.branches` di-ignore
- `refrences/` — CSV referensi WHO (sumber data `tabel.ts`; CATATAN: folder ditulis `refrences`, bukan `references`)
- `.env` — `VITE_SUPABASE_URL` & `VITE_SUPABASE_ANON_KEY` (gitignored); templat di `.env.example`
- `vercel.json` — SPA rewrite agar deep-link tidak 404

## Data & Klasifikasi Status
- Indikator: BB/U (wfa), TB/U (lhfa), BB/TB (wfl < 24 bln, wfh >= 24 bln), LiKA (hcfa), LiLA (acfa)
- BB/U: SK (<-3), K (-3..-2), N (-2..+1), RBL (>+1)
- TB/U: SP, P, N, T (batas sama dgn BB/U)
- BB/TB (6 kategori): GB (<-3 "Gizi Buruk"), GK (-3..-2 "Gizi Kurang"), GN (-2..+1 "Gizi Baik"), RGL (>+1..+2 "Risiko Gizi Lebih"), GL (>+2..+3 "Gizi Lebih"), O (>+3 "Obesitas")
- LiKA: MS (<-2), N (-2..+2), MK (>+2); LiLA: GK (<-2), N (>= -2)
- BB/TB dicari berdasarkan panjang/tinggi badan (baris terdekat), bukan umur
- Umur < 24 bulan: tabel lhfa/wfl versi `2_years`; >= 24 bulan: versi `5_years`
- Umur < 13 minggu: tabel mingguan (`*_13_weeks.csv`, kolom `Week`) tersedia tapi belum dipakai
- Umur dihitung otomatis dari tanggal lahir vs tanggal kunjungan (kalender); bila tanggal lahir kosong, `umur_bulan` + status BB/U·TB/U·BB/TB disimpan `null` sampai tanggal lahir diisi
- Skor-z dibulatkan 2 desimal untuk tampilan; klasifikasi memakai skor-z mentah
- Rumus LMS: `l==0 → ln(x/M)/S`; selain itu `((x/M)^l - 1)/(l*s)`

## Modul Antropometri (standar WHO)
Sistem menerapkan klasifikasi antropometri **WHO Child Growth Standards (0–60 bulan)** dengan 3 indikator
independen + 2 indikator lingkar yang tidak pernah digabung menjadi satu z-score:
- **BB/U** — berat menurut umur (wfa): indikator berat; tidak dipakai untuk stunting/wasting/obesitas
- **TB/U (PB/U)** — panjang/tinggi menurut umur (lhfa): indikator pertumbuhan linear/stunting;
  `TB/U < -2` = stunting (`SP` = stunting berat, `P` = pendek) — **bukan** "gizi buruk"
- **BB/TB (BB/PB)** — berat menurut panjang/tinggi (wfl/wfh): indikator wasting/overweight/obesitas;
  lookup berdasarkan panjang/tinggi (baris terdekat), **bukan umur**
- **LiKA/U** — lingkar kepala menurut umur (hcfa): MS/MK
- **LiLA/U** — lingkar lengan atas (MUAC) menurut umur (acfa): GK/N

Pemilihan PB vs TB mengikuti umur (< 24 bln → PB, >= 24 bln → TB); `measurement_type` eksplisit
belum disimpan di DB (kolomnya tidak ada). Status disimpan **terpisah per indikator** sebagai label
Indonesia (mis. `Normal`, `Pendek`, `Gizi Buruk`) + z-score mentah (untuk kurva).

Referensi data WHO (`tabel.ts` dari `refrences/*.csv`) **dipisah dari logika klasifikasi**; pembaruan
standar cukup mengganti data tabel tanpa mengubah fungsi klasifikasi. Validasi sebelum klasifikasi:
jk wajib, tanggal lahir wajib valid, tanggal pengukuran tidak boleh sebelum tanggal lahir, BB > 0,
PB/TB > 0; data tidak valid → tidak diklasifikasi (error).

## Konvensi
- Bahasa Indonesia untuk kode, komentar, dan pesan
- Class status: string pendek seperti `SK`, `K`, `N`, `RBL`, `SP`, `P`, `T`, `GB`, `GK`, `GN`, `RGL`, `GL`, `O`
- Git commit: `(Update vX.Y.Z) Deskripsi singkat`. Konvensi versi (sejak v2.30.0): angka **minor** (X.Y.0) untuk batch fitur besar; angka **patch** (X.Y.Z>0, mis. v2.30.1/v2.30.2) untuk perbaikan kecil/sinkronisasi/tweak UI — hindari lompat minor untuk perubahan tipis
- Seluruh perhitungan di sisi klien (browser) — jangan pindahkan ke server tanpa alasan
- Komponen UI baru: pakai primitif shadcn-vue di `src/components/ui/` bila ada, selain itu pola cva (class-variance-authority)
- Tema: palet hijau klinis di `src/style.css` (`:root` + `@theme inline`); jangan hardcode warna.
  `--primary` = `#047857` (emerald-700, kontras putih ≥ 4.5:1 / AA) — jangan digelapkan/diterangkan lagi;
  input form memakai `h-12 md:h-10` (48px mobile utk target sentuh, 40px desktop) via konstanta `klsInput`.

## Konvensi Git & Perawatan Repo
- JANGAN commit `node_modules/`, `dist/`, `.env`, `venv/`, `__pycache__/`, `instance/*.db`
- Jangan memodifikasi file CSV referensi WHO tanpa verifikasi skema kolom
- `src/lib/kalkulator/tabel.ts` dihasilkan dari `refrences/*.csv` — regenerasi lewat skrip, bukan edit manual
- Saat mengubah kalkulator, jalankan `npm test` (validasi vs fixture Python)
- Ketika menambah dependensi npm, ikuti versi eksisting (vue-router v5, vite 8, tailwindcss 4)

## Masalah Dikenal / Catatan
1. Kalkulator TS sudah divalidasi vs fixture Python (20 kasus, toleransi z ±0.005)
2. `kesimpulan_bb_bulan_lalu` belum ada sebagai kolom; sejak v2.26.0 penilaian BB naik/tidak disarankan otomatis dari tabel KBM (`src/lib/kbm.ts`) membandingkan BB kunjungan sebelumnya — mode tambah mengisi otomatis (boleh dioverride manual), mode ubah menghormati nilai tersimpan
3. Umur < 13 minggu masih dihitung per bulan (tabel mingguan belum dipakai)
4. **Fase data aktif:** Supabase Auth (email/password) + RLS ketat sudah jalan. `anon` bisa landing, kalkulator & `/dashboard` (hub publik); `/balita*` butuh login (guard redirect). **Kebijakan akses admin-only (2026-08-28, v2.39.4):** seluruh tulis (insert/update/delete) di SEMUA modul (balita, bumil, apras, remaja — identitas maupun kunjungan) hanya boleh **admin** (`public.user_peran` + fungsi `is_admin()`); **kader non-admin read-only** (hanya SELECT). Bila kader perlu mengubah data, ia diberi akun dengan peran admin oleh pengelola. RLS menegakkan kebijakan ini di lapisan DB + gating UI `v-if="isAdmin"` menyembunyikan tombol tambah/ubah/hapus; migrasi `20260830000000_kunci_tulis_admin.sql` men-drop policy `kader_tambah/ubah/hapus` yang tersisa (balita/apras) agar replikasi fresh juga aman. Status disimpan sebagai label Indonesia (`Normal`, `Kurang`, `Gizi Buruk`, dst); `src/modules/balita/db.ts` + `src/lib/status.ts` memetakan kode↔label.
5. **Supabase email confirmation ON di remote** (`mailer_autoconfirm=false`) — akun baru lewat UI `/login` butuh konfirmasi email. Catatan: `supabase/config.toml` lokal memakai `enable_confirmations = false` (tanpa konfirmasi), jadi perilaku lokal vs remote berbeda. Akun uji `e2e-kader@posyandu.test` dibuat manual (email_confirmed_at terisi + semua kolom token auth diisi string non-NULL).
6. **Bug GoAuth scan:** bila kolom token di `auth.users` NULL (mis. user dibuat manual), login gagal dengan "Database error querying schema" / "converting NULL to string". Workaround: isi `confirmation_token`, `recovery_token`, `email_change`, dll dengan string kosong.
7. **Akun kader seed (v2.14.0):** `posyandu@wapalo.com` dibuat langsung di database via migrasi `supabase/migrations/20260823000000_seed_akun_kader.sql` — **peran `petugas` (user biasa, read-only)**, bukan admin; semua kolom token diisi string kosong sesuai workaround #6 + `email_confirmed_at` terisi (email confirmation remote ON). Pendaftaran mandiri via UI sudah dihapus.
8. **Migrasi:** `supabase/migrations/` dipakai untuk replikasi fresh; perubahan yang sudah diterapkan ke remote dicatat di `supabase_migrations.schema_migrations`. Jalur CLI `db push` butuh password DB — alternatif non-interaktif yang terbukti: **Management API** (`POST /v1/projects/<ref>/database/query` dengan access token) atau dashboard SQL Editor. Migrasi ditulis **idempotent** (`add column if not exists` / `drop policy if exists` + `create policy` / `drop trigger if exists` + `create trigger`) agar aman dijalankan ulang. Status per v2.38.7 — **semua 18 migrasi file sudah diterapkan ke skema remote** (tabel & RPC terverifikasi lengkap): `20260826000000_bumil_rekap_kolom` (v2.31.1), `20260827000000_apras_rls_admin` (v2.33.1; policy admin apras terverifikasi — pencatatannya baru ditambahkan di v2.38.7), serta `20260828000000_balita_sakit_dirujuk` & `20260828000001_bumil_kategori_di_kunjungan` (diterapkan via Management API di v2.38.7). **Migrasi v2.39.0 `20260829000000_modul_remaja.sql`** (tabel `remaja_identitas` + RLS kader baca/admin tulis + trigger) **sudah diterapkan ke remote** via Management API (kolom/policy/trigger terverifikasi, terekam di `schema_migrations`). **Migrasi `20260830000000_kunci_tulis_admin.sql` (v2.39.4)** — kebijakan tulis admin-only di **semua** modul; men-drop policy `kader_tambah/ubah/hapus` yang tersisa di balita (`kader_tambah/ubah/hapus_balita`, `..._kunjungan`) & apras (`..._apras`, `..._apras_kunjungan`) agar replikasi fresh tidak membuka celah non-admin tulis; **sudah diterapkan ke remote** via Management API + dicatat di `schema_migrations` (policy tulis kader terverifikasi hilang, policy `kader_baca_*` dipertahankan). **Migrasi `20260830000001_modul_remaja_kunjungan.sql` (v2.40.0)** — tabel `remaja_kunjungan` (27 kolom) + RLS admin-only (`kader_baca_remaja_kunjungan` SELECT, `admin_tambah/ubah/hapus_remaja_kunjungan`) + trigger `trg_remaja_kunjungan_dibuat_oleh` (via `set_dibuat_oleh()`); **sudah diterapkan ke remote** via Management API + terverifikasi + dicatat di `schema_migrations`. **Migrasi `20260831000000_remaja_kunjungan_imt_teks.sql` (v2.40.1)** — drop kolom `status_gizi` + ubah `imt` numeric→teks (untuk format "19 (N)") — **sudah diterapkan ke remote** via Management API + dicatat di `schema_migrations`. **Catatan temuan (v2.38.7):** ~10 migrasi awal TIDAK tercatat di `schema_migrations` meskipun objek skemanya ada (proyek di-bootstrap dengan skema penuh, pencatatan sebagian) — entri yang ada memakai format timestamp tanpa nama file; disarankan audit `supabase db push --dry-run` bila password DB CLI tersedia.
9. Registry shadcn-vue tidak terjangkau dari environment ini — komponen `src/components/ui/` disalin manual dari repo `unovue/shadcn-vue` (branch `dev`, registry `new-york-v4`)
10. **Spacing CardContent:** komponen `CardContent` hanya merender `<div class="px-6">` tanpa flex/grid — `gap-*` di class-nya tidak bekerja. Gunakan `CardContent class="flex flex-col gap-N"` bila ingin jarak antar anak (telah diterapkan di KalkulatorView/LoginView/DashboardView/BalitaListView/BalitaFormView/BalitaDetailView; `gap-0` di LandingView sengaja tanpa jarak).
11. **Statistik publik (landing):** anonim tidak punya SELECT ke tabel data anak (RLS ketat), jadi statistik landing memakai fungsi `public.statistik_publik()` (SECURITY DEFINER, `search_path=public`, revoke public + grant anon/authenticated) yang **hanya mengembalikan COUNT agregat** (`balita_bayi`/`balita_balita`, `bumil_hamil`/`bumil_menyusui`, `kunjungan_balita_bulan_ini` (distinct), `kunjungan_bumil_bulan_ini` (distinct), `kunjungan_bulan_ini` (semua baris bulan berjalan), `bulan_ini`) — tanpa data perorangan. Persentase kunjungan Balita & Bumil/Menyusui dihitung di client. Landing fetch via `supabase.rpc('statistik_publik')` di `onMounted`; bila env Supabase kosong/error → seksi statistik disembunyikan.
12. **Kunjungan terakhir (dashboard kader):** seksi "Balita yang perlu perhatian" memakai fungsi `public.kunjungan_terakhir()` (**SECURITY INVOKER** — RLS tetap berlaku) yang mengembalikan kunjungan terakhir per balita (nama, tanggal lahir/kunjungan, status BB/U·TB/U·BB/TB). Hanya `authenticated` yang boleh execute (`revoke public` + `revoke anon` eksplisit + `grant authenticated`); Supabase auto-grant EXECUTE ke anon/authenticated sehingga perlu `revoke ... from anon` tambahan. Anon yang memanggil mendapat 401 permission denied. Filter "perlu perhatian" (Kurang/Sangat Kurang/Pendek/Sangat Pendek/Gizi Buruk/Gizi Kurang) dilakukan di client (`balitaPerluPerhatian()`).
13. **Limitasi antrean offline (sengaja):** `src/lib/offlineAntre.ts` hanya mengantrekan **tambah** kunjungan (Balita/Bumil/Apras **dan Remaja sejak v2.40.0** — `ModulAntre` = `'balita' | 'bumil' | 'apras' | 'remaja'`, sinkron lewat `tambahKunjunganRemaja`); **edit/hapus** kunjungan tidak pernah masuk antrean dan wajib online (mencegah konflik versi). Arah pengembangan: perluasan antrean operasi (edit/hapus ter-antri) di fase lanjutan — butuh desain deteksi konflik/resolusi sebelum diimplementasikan.
14. **Accepted-risk `xlsx@0.18.5` (v2.38.8):** npm audit melaporkan 1 vuln **high** di `xlsx` 0.18.5 (SheetJS CE): Prototype Pollution (`GHSA-4r6h-8v6p-xvw6`) & ReDoS (`GHSA-5pgg-2g8v-p4x9`), **tanpa fix di npm** (versi patched 0.20+ hanya didistribusikan via `cdn.sheetjs.com`). **Keputusan: dipertahankan (accepted risk)** karena pemakaian kita **write-only** — hanya `aoa_to_sheet`/`book_new`/`book_append_sheet`/`writeFile`/`sheet_to_csv` di `src/modules/*/ekspor.ts`, memuat data **internal** dari DB (bukan parse berkas user), sehingga kedua CVE tidak praktis dieksploitasi. Catatan: `cdn.sheetjs.com` tidak terjangkau dari environment kerja — remediasi ke versi patched butuh mengunduh `xlsx.full.min.js` dari CDN di mesin yang bisa mengaksesnya lalu di-vendor, atau migrasi ke paket alternatif (mis. `exceljs`).**

## Kolaborasi Tim

- **Awan** — Founder & pemilik produk: penentu arah, keputusan akhir, dan pemilik visi.
- **OpenCode CLI** — Software Engineer otonom (model bawaan, saat ini `big-pickle`):
  bekerja langsung dengan Awan di dalam repo ini dan patuh pada AGENTS.md; bertugas meneliti,
  merencanakan, mengimplementasikan, memverifikasi (build/test/lint), dan melaporkan hasil
  ke Awan.
- Alur kerja: Awan memberi misi → OpenCode riset & mematangkan spesifikasi kerja (tujuan,
  cakupan perubahan, file/komponen terpengaruh, kriteria penerimaan, catatan risiko) →
  implementasi → verifikasi (vue-tsc + vite build, vitest, eslint) → laporan hasil + risiko
  tersisa ke Awan → Awan memutuskan langkah berikutnya.
- Bahasa tim: **Indonesia** di semua komunikasi, kode, komentar, dan commit.
- Setiap perubahan perilaku aplikasi: perbarui PRD.md dan AGENTS.md agar tetap sinkron dengan
  kode, lalu commit dengan konvensi `(Update vX.Y.Z) Deskripsi`.
- Kontrol versi: **commit** dibuat oleh OpenCode saat diminta Awan (konvensi
  `(Update vX.Y.Z)`); **push** ke remote hanya dilakukan atas instruksi eksplisit dari Awan.

### Alur Pengembangan Fitur

1. **Awan** memberikan ide/misi dalam bahasa bebas (mentah, belum tentu terstruktur).
2. **OpenCode** mematangkan ide tersebut menjadi spesifikasi kerja: tujuan, cakupan perubahan,
   file/komponen yang terpengaruh, kriteria penerimaan, dan catatan risiko — mengonfirmasi
   ambiguitas ke Awan sebelum mulai bila perlu.
3. **OpenCode** mengimplementasikan sesuai spesifikasi, patuh pada AGENTS.md & PRD.md.
4. **OpenCode** memverifikasi hasil: `npm run build` (vue-tsc + vite), `npm test` (vitest),
   `npm run lint` (eslint), review diff, dan memastikan PRD.md/AGENTS.md disinkronkan dengan
   perubahan perilaku.
5. **Awan** menerima laporan hasil + risiko tersisa (jika ada) dan memutuskan langkah berikutnya.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- Dirty graphify-out/ files are expected after hooks or incremental updates; dirty graph files are not a reason to skip graphify. Only skip graphify if the task is about stale or incorrect graph output, or the user explicitly says not to use it.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).
